node_modules/
.next/
.next-prod/
.next-turbo/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env
.env.local
.env*.local
next-env.d.ts
tsconfig.tsbuildinfo

# Local snapshot / rules store written at runtime
.data/

# Backup copies of any file. Note that `.env*.local` does NOT match
# `.env.local.bak`, so secrets files need this rule as well as their own.
*.bak
*.backup
*.orig
.env.*.bak
